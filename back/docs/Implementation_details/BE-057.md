# BE-057：Scoring 雛形（metrics算出I/O）

## 担当者
- 萩原

## 指示
- `computeMetrics(...)` のシグネチャ作成
- 戻り値は `metrics` 形に合わせる

## 作業ログ
- `src/services/analysis/scoring.ts` に `computeMetrics(...)` を実装
- `ComputeMetricsInput` / `MetricSignal` の I/O 型を定義
- 戻り値を `AnalysisMetrics` 形（`noEvidenceClaimsCount` 等）に合わせた
