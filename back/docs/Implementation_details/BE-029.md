# BE-029：StorageService.putJson

## 担当者
- 萩原

## 指示
- JSON文字列を指定パスに保存
- `contentType: application/json` を指定
- 返り値は `gs://` path

## 作業ログ
- `putJson` を実装し、指定 object path に JSON 文字列を保存
- `contentType: application/json` を明示して保存
- 返り値を `gs://{bucket}/{objectPath}` で返す実装にした
