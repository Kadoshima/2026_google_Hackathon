# BE-037：GET /v1/analysis/:analysisId（進捗ポーリング）

## 担当者
- 萩原

## 指示
- `FirestoreRepo.getAnalysis` で取得
- READYなら `summary/pointers` を返す
- `analysis_json_signed_url` を返す

## 作業ログ
- `src/routes/v1/analysis.ts` に `GET /analysis/:analysisId` を実装
- `FirestoreRepo.getAnalysis` で取得し、未存在時は 404 を返す処理を追加
- `READY` の場合に `summary/pointers` を付与し、`analysis_json_signed_url` を含む signed URL を返す実装にした
