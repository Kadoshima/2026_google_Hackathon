# BE-010：Honoサーバ雛形（entry/server/route登録）

## 担当者
- 萩原

## 指示
- `src/index.ts` で `serve({ fetch: app.fetch, port })` を用意する
- `src/server.ts` で `/v1` と `/internal` を route 登録する
- エンドポイント実装は入れない（空ルートでOK）

## 作業ログ
- `src/index.ts` で Hono の entry (`serve` 呼び出し) を作成
- `src/server.ts` で `/v1` と `/internal` の route 登録を実装
