# BE-071：POST /v1/oral/ask（質問→評価→追記文案→todo候補）

## 担当者
- 

## 指示
- request/response の型を作る
- `OralExaminer` を呼ぶだけでOK
- 保存ONなら conversation 保存

## 作業ログ
- `back/src/routes/v1/oral.ts` に `POST /v1/oral/ask` を実装
- request validation、`OralExaminer` 呼び出し、`SAVE` ポリシー時の conversation 保存を追加
