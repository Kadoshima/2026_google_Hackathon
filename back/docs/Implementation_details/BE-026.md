# BE-026：FirestoreRepo.getAnalysis

## 担当者
- 

## 指示
- `analysisId` で1件取得
- 存在しない場合は `null` を返す

## 作業ログ
- `src/services/firestore.repo.ts` に `GetAnalysisInput` と `getAnalysis` を実装
- `analyses/{analysisId}` を取得し、存在しない場合は `null` を返す実装にした
