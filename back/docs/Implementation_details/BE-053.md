# BE-053：Analysis Orchestrator 雛形（extract→analysis→finalize）

## 担当者
- 萩原

## 指示
- `run(analysisId)` の流れだけ作る
- `updateAnalysisStatus` を呼んで progress 更新
- 具体処理は TODO で残す

## 作業ログ
- `src/services/analysis/orchestrator.ts` に `run(analysisId)` フローを実装
- `updateAnalysisStatus` で `EXTRACTING -> ANALYZING -> READY` の進捗更新を追加
- 実処理本体は BE-054〜057/043〜045 に委譲する TODO コメントを配置
