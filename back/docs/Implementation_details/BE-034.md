# BE-034：POST /v1/upload（multipart受理＋metadata parse）

## 担当者
- 萩原

## 指示
- `file` と `metadata` を受け取る
- `metadata` は JSON.parse して型チェック
- ZIP/PDF の拡張子チェックだけ先に

## 作業ログ
- `src/routes/v1/upload.ts` に `POST /upload` を実装
- multipart の `file` / `metadata` を受理し、`metadata` を JSON.parse + 型検証
- ファイル拡張子を `.zip` / `.pdf` のみ許可する入力チェックを実装
