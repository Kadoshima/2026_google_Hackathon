# BE-027：GCS 初期化（client生成）

## 担当者
- 萩原

## 指示
- `@google-cloud/storage` で client を作る
- `BUCKET_NAME` を env で読む
- 一箇所にまとめて export する

## 作業ログ
- `src/services/storage.service.ts` に `Storage` client 初期化を実装
- `GCP_PROJECT_ID` と `BUCKET_NAME` の env 参照を追加
- Storage操作関数を同一モジュールから export する構成に整理
