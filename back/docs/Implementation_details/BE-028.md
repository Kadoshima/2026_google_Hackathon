# BE-028：StorageService.putRawFile

## 担当者
- 萩原

## 指示
- `raw/{sessionId}/{submissionId}/...` に保存
- 返り値は `gs://` path
- MIME type を設定する

## 作業ログ
- `putRawFile` を実装し、`raw/{sessionId}/{submissionId}/...` 配下へ保存
- `contentType` を保存時に指定する処理を追加
- 返り値を `gs://{bucket}/{objectPath}` 形式に統一
