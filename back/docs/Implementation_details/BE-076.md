# BE-076：GET /v1/report/:reportId（署名URL返却）

## 担当者
- 

## 指示
- reportId から pointer を引く
- signed URL を返す

## 作業ログ
- `back/src/routes/v1/report.ts` に `GET /v1/report/:reportId` を実装
- Firestore から pointer を引き、`report_html_signed_url` を返す処理を追加
