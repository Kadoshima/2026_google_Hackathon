# BE-023：FirestoreRepo.setPointers

## 担当者
- 

## 指示
- `gcsExtractJson/gcsAnalysisJson/gcsReportHtml` を更新
- 既存の `pointers` を merge する

## 作業ログ
- `src/services/firestore.repo.ts` に `SetPointersInput` と `setPointers` を実装
- `analyses/{analysisId}` を読み出して既存 `pointers` をマージし、`merge: true` で更新
- `updatedAt` も同時に更新
