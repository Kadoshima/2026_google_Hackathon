# BE-030：StorageService.getSignedUrl

## 担当者
- 萩原

## 指示
- 読み取り用 signed URL を返す
- TTL は `SIGNED_URL_TTL_SECONDS` から取得
- 返り値は URL string

## 作業ログ
- `getSignedUrl` を実装し、読み取り用 V4 signed URL を返すようにした
- `SIGNED_URL_TTL_SECONDS` を env から取得し、有効期限に反映
- `gs://` 形式のパス解析ヘルパーを追加して入力検証を実装
