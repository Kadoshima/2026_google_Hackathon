# BE-036：POST /v1/analyze（analysis作成→Cloud Tasks投入）

## 担当者
- 萩原

## 指示
- `analysis_id` を採番して保存
- `TasksService.enqueueAnalysisTask` を呼ぶ
- response: `{ analysis_id }`

## 作業ログ
- `src/routes/v1/analyze.ts` に `POST /analyze` を実装
- `analysis_id` を採番し `FirestoreRepo.createAnalysis` で初期レコードを保存
- `TasksService.enqueueAnalysisTask` を呼び出し、response `{ analysis_id }` を返す処理を追加
