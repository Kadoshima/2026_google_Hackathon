# BE-019：FirestoreRepo.createSession

## 担当者
- 

## 指示
- `sessionId`, `clientTokenHash`, `retentionPolicy` を保存
- `createdAt/updatedAt` をセット
- 保存先は `sessions/{sessionId}`

## 作業ログ
- `src/services/firestore.repo.ts` に `createSession` を追加
- 引数は `CreateSessionInput` として定義し、`sessionId/clientTokenHash/retentionPolicy` を受け取る
- `createdAt/updatedAt` を現在時刻でセットし、`sessions/{sessionId}` に保存
