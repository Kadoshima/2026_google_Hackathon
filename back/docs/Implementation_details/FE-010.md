# FE-010：SPAルーティング骨格（/new, /session/:id, /settings, /demo）

## 担当者
- 萩原

## 指示
- ルーティングの基本導線を作る

## 作業ログ
- Next App Router で `/new`, `/session/[sessionId]`, `/settings`, `/demo` を実装
- `TopNav` / `SideNav` / `AppShell` で共通レイアウトを提供
- セッション詳細画面のタブ切替を `?tab=` でルーティング

