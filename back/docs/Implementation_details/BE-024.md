# BE-024：FirestoreRepo.setMetrics

## 担当者
- 

## 指示
- `noEvidenceClaimsCount/weakEvidenceClaimsCount/specificityLackCount` を更新
- 既存の `metrics` を merge する

## 作業ログ
- `src/services/firestore.repo.ts` に `SetMetricsInput` と `setMetrics` を実装
- `analyses/{analysisId}` の既存 `metrics` を読み出し、指定項目のみマージ更新
- `updatedAt` を同時更新
