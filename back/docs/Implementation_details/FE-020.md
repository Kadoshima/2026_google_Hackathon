# FE-020：SummaryTab（致命傷Top3 + metrics Before/After枠）

## 担当者
- 萩原

## 指示
- Summary タブを実装し、読みやすい日本語UIにする
- 重大リスクTop3を中心に、縦積み情報を抑制する
- Understanding Score導入を見据えて拡張可能な構成にする

## 作業ログ
- Session 画面に Summary タブを追加
- `top3_risks` と `metrics` を表示する UI を実装
- Before/After 枠（比較表示）を追加
- 追加で `top_risks`（複数件）表示に対応し、`top3_risks` は後方互換用にフォールバック
- Summaryに「全体所見」ブロックを追加（HIGH/MEDIUM/LOW件数、Preflight件数、弱根拠件数）
- リスクカードに severity / reason / refs 情報を明示し、次の修正方針がわかるUIに改善
- 表示文言を日本語中心に統一
- 将来の `UnderstandingScoreCard` を追加しやすいレイアウトに整理
