# BE-018：Firestore 初期化（client生成）

## 担当者
- 

## 指示
- `@google-cloud/firestore` の client を初期化
- env: `GCP_PROJECT_ID`, `FIRESTORE_DB` を読む
- 一箇所にまとめて export する

## 作業ログ
- `src/services/firestore.repo.ts` で Firestore client 初期化を実装
- env `GCP_PROJECT_ID` を必須、`FIRESTORE_DB` を任意で読み込み
- `firestore` を export して repository 層で共通利用する形にした
