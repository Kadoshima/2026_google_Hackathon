# BE-054：EvidenceAuditor 雛形（I/O定義）

## 担当者
- 萩原

## 指示
- 入出力の型を定義し、最小でも動作する判定ロジックを実装
- `claimId` と根拠段落IDの対応が返ること

## 作業ログ
- `src/services/analysis/evidenceAuditor.ts` に入力型・出力型を定義
- `claimId` と `paragraphIds` を含む `EvidenceRisk` モデルを追加
- `auditEvidence(...)` を実装
  - テキスト正規化 + トークナイズ
  - claim と paragraph の語彙オーバーラップで関連度を算出
  - 閾値に応じて `HIGH/MEDIUM/LOW` のリスクを生成
- 追加で「一致度平均 / トークンカバー率 / 定量シグナル有無」を評価軸に導入
- リスク理由文を日本語で具体化（割合・不足観点を明示）し、修正指示に直結する形へ改善
