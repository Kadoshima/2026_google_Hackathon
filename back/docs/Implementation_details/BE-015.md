# BE-015：ConversationTurn 型の作成

## 担当者
- 

## 指示
- `analyses/{analysisId}/conversations` の項目を型にする
- `role/type/content/refs` を含める
- `refs` は `{ paragraphIds, claimIds, figureIds, citationKeys }` を配列で

## 作業ログ
- `src/domain/types.ts` に `ConversationRefs` と `ConversationTurn` 型を追加
- `role/type/content/refs` を要件どおりに定義し、`createdAt` を追加
