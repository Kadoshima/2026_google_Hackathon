# BE-081：Agent Trace保存と進捗API拡張（summary.agents リアルタイム返却）

## 担当者
- 萩原

## 指示
- Agentの進行状況を解析中にも追えるようにする
- 進捗APIで `summary.agents` を READY前から返せるようにする
- フロントのライブ進捗表示に必要な最小情報を返却する

## 作業ログ
- Domain型を拡張
  - `AgentRole`, `AgentStatus`, `AgentTraceEntry`
  - `Analysis.agentTrace` を追加
- FirestoreRepoに `setAgentTrace` を追加
- Orchestratorで各Agent完了ごとに `agentTrace` を Firestoreへ同期
- `GET /v1/analysis/:analysisId` を拡張
  - READY前でも `summary.agents` を返却
  - ステップ別進行メッセージを日本語化

