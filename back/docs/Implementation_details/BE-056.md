# BE-056：PriorArtCoach 雛形（I/O定義）

## 担当者
- 萩原

## 指示
- 入出力の型を定義し、検索クエリ生成ロジックを実装
- claim ごとに query + rationale を返す

## 作業ログ
- `src/services/analysis/priorArtCoach.ts` に入出力型を定義
- claimごとの検索クエリを返す `PriorArtQuery` モデルを追加
- `proposePriorArtQueries(...)` を実装
  - claim からキーワード抽出
  - `domainTag` があれば query 先頭に付与
  - 検索意図を `rationale` に保持
