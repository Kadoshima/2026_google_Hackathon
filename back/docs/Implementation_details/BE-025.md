# BE-025：FirestoreRepo.saveConversationTurn

## 担当者
- 

## 指示
- subcollection `analyses/{analysisId}/conversations/{turnId}` に保存
- `createdAt` を付与
- 保存ONの時だけ呼ぶ前提

## 作業ログ
- `src/services/firestore.repo.ts` に `SaveConversationTurnInput` と `saveConversationTurn` を実装
- `analyses/{analysisId}/conversations/{turnId}` に `ConversationTurn` を保存
- `createdAt` を付与して保存後、保存オブジェクトを返却
