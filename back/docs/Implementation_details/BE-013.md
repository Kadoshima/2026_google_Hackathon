# BE-013：Submission 型の作成

## 担当者
- 

## 指示
- `submissions/{submissionId}` の項目を型にする
- `inputType` は Enum を使う
- `status` は最小で `UPLOADED/DELETED` を含める

## 作業ログ
- `src/domain/submissions.ts` に `SubmissionStatus` と `Submission` 型を追加
- `inputType` は `InputType`（`enums.ts`）を参照するように定義
