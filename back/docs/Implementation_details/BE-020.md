# BE-020：FirestoreRepo.createSubmission

## 担当者
- 

## 指示
- `submissionId`, `sessionId`, `inputType`, `gcsPathRaw` を保存
- `status=UPLOADED` で作成
- `createdAt` をセット

## 作業ログ
- `src/services/firestore.repo.ts` に `CreateSubmissionInput` を追加
- `submissionId/sessionId/inputType/gcsPathRaw` を受け取り、`status=UPLOADED` と `createdAt` を付与して `submissions/{submissionId}` に保存する `createSubmission` を実装
