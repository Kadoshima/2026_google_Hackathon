# BE-073：POST /v1/patch/generate（採用ToDo→diff生成→GCS保存）

## 担当者
- 

## 指示
- request/response 型を作る
- PatchService を呼ぶだけでOK
- diff URL を返す

## 作業ログ
- `back/src/routes/v1/patch.ts` に `POST /v1/patch/generate` を実装
- diff を GCS (`patches/...`) に保存し、`diff_signed_url` を返す処理を追加
