# BE-055：LogicSentinel 雛形（I/O定義）

## 担当者
- 萩原

## 指示
- 入出力の型を定義し、具体性不足の最小判定ロジックを実装
- ルールベースで `HIGH/MEDIUM/LOW` を返す

## 作業ログ
- `src/services/analysis/logicSentinel.ts` に入出力型を定義
- `claimId` ベースで返す `LogicRisk` を追加
- `inspectLogic(...)` を実装
  - 数値欠如、絶対表現、比較基準欠如のパターンを検出
  - 検出理由を `reason` に保持
  - 重大度を `HIGH/MEDIUM/LOW` で返却
- 理由文を日本語化し、「何が足りないか（条件/数値/比較対象）」がそのまま読める形に改善
