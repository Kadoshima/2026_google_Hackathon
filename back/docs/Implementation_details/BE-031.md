# BE-031：Cloud Tasks 初期化（client生成）

## 担当者
- 萩原

## 指示
- `@google-cloud/tasks` client を初期化
- `TASK_QUEUE_NAME`, `TASK_LOCATION` を env で読む
- queue path を作る関数を用意

## 作業ログ
- `src/services/tasks.service.ts` で `CloudTasksClient` 初期化を実装
- `TASK_QUEUE_NAME` と `TASK_LOCATION` を含む env 検証を追加
- queue path を返す `getAnalysisQueuePath` を実装
