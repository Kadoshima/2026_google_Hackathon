# BE-012：Session 型の作成

## 担当者
- 

## 指示
- `sessions/{sessionId}` の項目を TypeScript 型にする
- `retentionPolicy` は `{ mode, ttlHours }` を含める
- `createdAt/updatedAt` は `Date | string` でOK（後で統一）

## 後輩向けヒント
- README の「6.1 Firestore > sessions/{sessionId}」の項目をそのまま写す
- 具体例: `sessionId`, `clientTokenHash`, `retentionPolicy`, `language`, `domainTag`, `createdAt`, `updatedAt`
- `retentionPolicy.mode` には `RetentionMode` を使う（`enums.ts` から import）

## 作業ログ
- `src/domain/types.ts` に `Session` 型を追加（`sessionId`, `clientTokenHash`, `retentionPolicy`, `language`, `domainTag`, `createdAt`, `updatedAt`）
- `RetentionPolicy` を `RetentionMode` 連携の型として定義
